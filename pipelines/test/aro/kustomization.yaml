apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      deployment: kuadrant-qe-pipeline
      app: test-aro

components:
  - ../../../tasks/login/
  - ../../../tasks/deploy/
  - ../../../tasks/infra/
  - ../../../tasks/test/

resources:
  - pipeline.yaml
