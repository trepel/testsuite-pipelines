apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      deployment: kuadrant-qe-pipeline
      app: test-nightly

components:
  - ../../../tasks/login/
  - ../../../tasks/test/

resources:
  - pipeline.yaml
